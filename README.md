# Leaflet-drawing-template
A Leaflet Map with Drawing tools, import data tool and export the drawn data in to geoJson, Also can able to add attribute data to drawn items, edit drawn features and Geocoding
